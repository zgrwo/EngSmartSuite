# 属性控制图 — 计数型数据控制图 (p/np/c/u)
task: spc_attribute
target_col: "首件合格"
feature_cols:
  - "班次"        # 横坐标 (批次/日期/类别，空则用顺序)
params:
  chart_type: p   # p=不良率, np=不良数, c=缺陷数, u=单位缺陷率
  group_col:      # 分组依据 (可选，按此列分系列线)
  # n_col: "检验数"  # 样本量列 (p/u 图变样本量时需要)
