# EngSmartSuite 全量深度审查报告

| 元数据 | 值 |
|---|---|
| 审查日期 | 2026-08-29 |
| 审查基线 | commit `c43be41`（fix(engine): 修复全量验证发现的 3 个 MED 问题） |
| 审查范围 | `src/smartsuite/`（core/engine/services/web/cli）、`tests/`、`scripts/`、`rules/`、`templates/` |
| 审查方法 | 自动化门禁全量 + 4 路并行专项审查（架构/防错/文档/测试）+ 高危结论实机复核 |

## 1. 审查环境

| 项 | 值 |
|---|---|
| OS | Windows（幂等 PowerShell 5.1） |
| Python | 3.14.5 |
| 测试命令 | `python scripts/verify_all.py`、`pytest tests/ -q`、`ruff check/format` 等 |

## 2. 自动化门禁实测结果

| 门禁 | 命令 | 结果 |
|---|---|---|
| 单元/集成测试 | `pytest tests/ -q` | ✅ **664 passed / 4 skipped**（21 warnings，218s） |
| 代码风格 | `ruff check src/smartsuite/ scripts/ tests/` | ✅ 全绿 |
| 格式 | `ruff format --check ...` | ✅ 81 文件已格式化 |
| 一键全量验证 | `python scripts/verify_all.py` | ❌ **FAIL**（仅文档一致性一项） |
| 文档一致性 | `python scripts/verify_docs.py --strict` | ❌ **FAIL**：`tests/test_data_u.xlsx` 未声明 |
| 文档一致性 | `scripts/verify_consistency.py` | ✅ 注册 41=41=41=41 一致 |
| 手册数值 | `scripts/verify_manual_claims.py` | ✅ 硬校验清单全过（10 项） |
| falsy 审计 | `scripts/falsy_audit.py` | ✅ 0 HIGH / 7 MEDIUM（人工核查为 set 判空误报）/ 75 LOW |
| 测试质量守卫 | `scripts/test_quality_guard.py` | ✅ 通过（WARN 17 处弱断言，非阻断） |

### 覆盖率（复测，Python 3.14.5）

| 模块 | 行覆盖 |
|---|---|
| root_cause.py（3907 行） | 82%（函数级 33/33） |
| doe_opt.py（2356 行） | 84%（函数级 31/31） |
| capability.py | 92% |
| detection.py | 84% |
| spc_charts.py | ~87% |
| exploratory.py | 89% |
| reliability.py | 88% |
| `_utils/_constants/_doe_arrays/_palette/spc_monitor` | 100% |

## 3. 合规性结论（项目红线）

### ✅ 全绿项

1. **架构分层**：engine/core 全量 import 扫描零 flask/xlwings/openpyxl/IO/shell 依赖；web 零 engine 直接导入；services/orchestrator 为唯一桥接；engine/core 无反向依赖。
2. **注册清单一致性**：TASK_REGISTRY / TASK_LABELS / TASK_GROUPS / DEFAULT_PARAMS / app.js TASK_PARAMS 五方键集全等；与 api-reference.md 41/41 无孤儿；templates YAML 44=43 任务+1 工作流指南。
3. **签名一致性**：api-reference 函数签名与代码一致（抽查 14 项默认值 + 30+ 返回表键全对）。
4. **无裸 except**：`except:` 零匹配；约 50 处 `except Exception` 全部有日志或重抛或返回错误值。
5. **退化输入走哨兵值**：空数据/全 NaN/常量列/单行/共线在 engine 各方法均有显式中文 error 或 NaN 哨兵兜底，无静默传播。
6. **测试 4 层防线真实存在**：① 数值正确性（强●29 / 中◐10 / 弱○2）② 数学不变量 ③ 边界 41 任务全循环 ④ CLI/Web 差分 41/41 单元格级比对 + 变异验证。

## 4. 问题清单（按严重度）

### 🔴 高

| # | 位置 | 问题 | 状态 |
|---|---|---|---|
| R1 | `tests/test_data_u.xlsx` | 281KB 残留产物：git 未跟踪、gitignore 未忽略、代码零引用，导致 verify_docs/verify_all **门禁 FAIL** | **已实测确认** |
| R2 | `rules/user-manual.md:1281` | trend_forecast `RMSE=2.495` 漂移，实测 **1.2415**；不在此硬校验清单内 | **已实测确认** |
| R3 | `engine/detection.py:760` | Isolation Forest 失败将 sklearn 英文异常 `{e}` 直接拼入用户 `messages`，违反"中文工艺术语"红线（同行 756 行已有日志） | **已实测确认** |
| R4 | `tests/crossval_r/test_r_reference.py` / `rules/project-structure.md:85` | 声称的"R 4.3+ 参考对比（±1e-4）"实际不存在——文件已自认改标但 project-structure.md 仍标注"R 交叉验证参考实现"；api-reference 若仍承诺 R 需修订 | **已实测确认** |

### 🟡 中

| # | 位置 | 问题 |
|---|---|---|
| R5 | `engine/doe_opt.py:2354, 2012` | `messages=[str(e)]` 裸异常当用户文案，依赖 raise 来源"恰好是中文"——第三方英文 ValueError 即泄漏 |
| R6 | `engine/doe_opt.py:1134, 1194` | `str(e)[:60]` 原始异常嵌入用户可见的"效应量"表格单元格 |
| R7 | `rules/user-manual.md:1552, 1558` | §8.1 bootstrap CI 上限 `4.330` 漂移，实测 **4.3244**（点估计 4.2491/下限 4.1740 正确）；"重抽样 1000 次" vs 实际 `n_bootstrap=2000` |
| R8 | `engine/root_cause.py:1850-1889` | **Dunn 事后检验真实分支零测试覆盖**（需 kruskal p<α 且 ≥3 组才触发）；doe_design 非法 `n_runs` 分支亦缺测 |
| R9 | `tests/test_invariants.py` 多处、`tests/test_services/test_orchestrator.py:133/173` | 守卫架空（`if x is not None` 单分支）与恒真断言（`status in (ok,error)`）——守卫脚本自身已 WARN 点名 |
| R10 | `tests/test_engine/test_fuzz.py:1-19` | docstring 承诺"宽表/重复值"覆盖，实际无任何用例（宽表全仓缺口） |
| R11 | `web/api.py` vs `services/reporter.py`；`cli.py:144-193` vs `web/api.py:117-160` | 图表 base64 序列化两套平行实现；CLI/Web 编排胶水序列重复，靠差分测试兜底 |
| R12 | `rules/user-manual.md:536` | §5.1 表名 `kappa_results` vs 代码 `kappa_result`（api-reference 正确） |

### 🟢 低

| # | 位置 | 问题 |
|---|---|---|
| R13 | `services/reporter.py:96-97` | 唯一 `except Exception: continue` 无日志：中文字体注册全部失败时静默落 Helvetica（报告中文变空白，用户与日志均无线索） |
| R14 | `engine/spc_charts.py:2013/2021/2025/2039/2043` | guard 风格不统一：外层 `is not None`，内层 `if ucl_2s:` truthiness——限值恰为 0.0 时漏画 2σ 参考线 |
| R15 | `web/api.py:127-132` | 空数据 `ValidationError` 降级为非阻塞警告后仍进引擎，空路径防御依赖引擎分散守卫 |
| R16 | `engine/root_cause.py`（3907 行）、`engine/doe_opt.py`（2356 行） | 单文件跨多子域（统计检验/ML/功效/DOE/回归混排）；hypothesis_test 单函数约千行 |
| R17 | `engine/__init__.py` | 包级重型初始化（字体扫描+三大巨模块全量拉载）；子模块若写 `from smartsuite.engine import X` 将触发半初始化陷阱（当前未触发） |
| R18 | `cli.py`、`rules/project-structure.md` | cli.py 第五入口缺席 AGENTS.md 架构图；project-structure.md 未列 `test_med_fixes.py`/`test_upload_limits.py` |
| R19 | `ci.yml:130-131`、`scripts/README.md:35`、test docstrings、手册 §9.3 | 多处过时"40 任务/40/40"文案（实为 41） |
| R20 | `rules/api-reference.md` | `bootstrap_ci.random_state` 未入文档；`scatter_plot.show_ci` 字符串/布尔混用未标注 |

### ⚠️ 复核记录（原疑项查证结论）

| 疑项 | 结论 |
|---|---|
| §8.3 gage_rr `%GRR=99.5%` 漂移（sub-agent 报告 97.46%） | **撤销指控**：实机复测 `grr_sv=99.49%`，与手册 99.5% 一致（ndc=0、判定不合格均正确） |
| §6.1 回归整表漂移（const=6.1049→6.081 等） | **判为"需复核"**：手册未记录示例特征集，无法构造等价复现；双方实测均对不上手册，证据不足不确认为缺陷 |
| `falsy_audit.py` 标记的 7 处 MEDIUM（`if r2:`~`if r8:`） | **确认误报**：r2-r8 均为 `set[int]` 判空（空集即该规则无违规），语义正确 |
| R8 Dunn 分支 | **确认缺测**：覆盖率缺行 L1850-1889、L2331-2345，纯人工构造用例才能触发 |

## 5. 修复建议与优先级

**P0（先修，恢复门禁绿）**
1. `tests/test_data_u.xlsx`：删除或加入 `.gitignore` 并同步 `project-structure.md` → 恢复 verify_docs/verify_all 通过。
2. `user-manual.md:1281` RMSE 改为 1.2415，并把 §7.6/§8.1 数值纳入 `verify_manual_claims.py` 硬校验清单（§8.3 实测正确，无需改）。

**P1（防御一致性）**
3. 统一收敛"引擎→用户消息"：`detection.py:760`、`doe_opt.py:1134/1194/2354` 改为固定中文文案，异常详情仅进日志，复用 orchestrator 的异常→中文映射机制。
4. `reporter.py:96` 补 `logger.warning`；`spc_charts.py` 内层 guard 统一 `is not None`。

**P2（测试补齐 + 文档核对）**
5. 补 Dunn 事后检验触发用例（构造 kruskal 显著+≥3 组数据）；补"宽表/重复值"fuzz 用例以兑现 docstring 承诺。
6. `test_invariants.py`/`test_orchestrator.py` 弱断言改双分支断言，哄不过守卫脚本。
7. 修订 R4（crossval_r 定位说明）、R12（表名）、R19（41 文案）、R20（api-reference 补项）。
8. `project-structure.md` 补 `test_med_fixes.py`/`test_upload_limits.py`/`cli.py` 声明，AGENTS.md 架构图补第五入口。

## 6. 审查结论

架构红线全合规、注册/签名/文档五方一致、四层测试防线真实有效、防错成熟度高（历史 falsy 族/preprocess 解包族已系统性根治）。当前**唯一阻断项是 `tests/test_data_u.xlsx` 残留导致门禁 FAIL**。核心待办集中在三类：① 手册未纳入硬校验的数值漂移 ×2（R2/R7）；② 引擎内散落的裸异常文本进入用户消息 ×3（R3/R5/R6，同一根因）；③ 测试守卫架空与真实分支缺测（R8/R9）。按 P0→P1→P2 顺序修复即可恢复全绿并消除系统性风险。

---
*报告依据：git c43be41 基线实测 + 2026-08-29 全量重跑门禁；所有"已实测确认"项均有本机复现数据支撑。*